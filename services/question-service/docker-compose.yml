services:
  postgres:
    image: postgres
    container_name: postgres
    environment:
      - POSTGRES_URL:jdbc:postgresql://postgres:5432/appdb
      - POSTGRES_DB=appdb
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=example
    ports:
      - "5432:5432"