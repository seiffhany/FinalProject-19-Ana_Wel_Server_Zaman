package com.example.user_service.models;

// This enum represents the different types of activities that a user can perform
public enum ActivityType {
    FOLLOW,
    QUESTION,
    ANSWER,
    COMMENT,
}
